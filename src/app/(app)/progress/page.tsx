import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function ProgressPage() {
  return (
    <PlaceholderPage
      title="Progress"
      description="This is where students will track completed study sessions, study time, tasks completed, streaks, XP, and level."
    />
  );
}
